
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Адрес = 
		"https://oauth.yandex.ru/authorize?response_type=code&client_id=" + Параметры.IDПриложения;
КонецПроцедуры

&НаКлиенте
Процедура АдресДокументСформирован(Элемент)
	Позиция = СтрНайти(Элементы.Адрес.Документ.URLUnencoded, "code=");
	Если Позиция Тогда 
		Оповестить("ПолучениеКодаАвторизации", Сред(Элементы.Адрес.Документ.URLUnencoded, Позиция + 5));
		Закрыть();
	КонецЕсли;
КонецПроцедуры
